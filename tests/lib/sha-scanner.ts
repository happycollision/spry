export const SHA_POOL: readonly string[] = [
  "3f8a2c91d4e6b0f7a5c2e8d1b9f3a7c4e0d6b2f8",
  "b47e1d05c9f2a8e3d7b0c4f1e6a2d8b5c3f9e1a7",
  "7c3d9e2f1a8b5c4d0e7f6a3b9c1d8e5f2a4b7c0d",
  "a1f4b8e2c7d3f9a5b0e6c4d1f8a3b7e9c2d5f0a6",
  "e5b0c3d8f2a7b4e1c9d6f3a0b8e4c2d7f5a1b9e3",
  "2d7f4a1b9e6c3d8f5a2b7e4c0d9f6a3b1e7c4d2f",
  "f1a9e4c2d7b5f0a8e3c6d1b9f4a2e7c5d0b3f8a1",
  "8e3b6f1c4d9a7e2b5f0c8d3a6b1e4f9c7d2a5b0e",
  "c5d2a9f6b3e0c7d4a1f8b5e2c9d6a3f0b7e4c1d8",
  "6a1e8b3d5f2c9a6e3b0d7f4c1a8e5b2d9f6c3a0e",
  "d9f3c0a7e4b1d8f5c2a9e6b3d0f7c4a1e8b5d2f9",
  "4b7e1d8f5c2a9b6e3d0f7c4b1e8d5f2c9a6e3b0d",
  "9c6a3f0e7d4b1c8f5a2e9d6c3b0f7a4e1d8c5b2f",
  "1e4d7b0c8f5a2e9d6c3a0f7b4e1d8c5b2f9a6e3d",
  "5f2a8d1e4b7c0f3a6d9e2b5c8f1d4a7e0b3c6f9d",
  "0b3c6f9d2a5e8b1d4c7f0a3e6d9c2b5f8a1d4c7e",
  "e7d4a1c8b5f2e9c6a3d0b7e4c1f8a5d2b9e6c3f0",
  "8c1f5a9d2e7b4c0f6a3e8d1b5c9f2a7e4d0b6c3f",
  "30877432d1026706d7e805da846a32c3bb81e3c2",
  "9b62179273c8eb5bb682575ec87a171ac826a6fc",
  "e48478dcb74f21345d2cce8038a39d5e0853964b",
  "50af03b971722f244f58d669cbee3772a0770217",
  "21a278f64f7fd633dbdde131ca3766e4d58e72e3",
  "10275dff6c15c0c8e9df469611a11f5125227c37",
  "12da86a78c49ea20e32684b27b95e90934833489",
  "6a68f812d810a485ed03241b4d419b1b673bd475",
  "5d05ad7853c1f76eb97706ca828bca0385813dba",
  "d3c681d06bd2aa399dac946dc59c0996daeee6f5",
  "29a279764017f2ed6cfc7403d75e173e4eaede5f",
  "e878f78e2978aa2447c462ddaed16dc0cf0b9cd7",
  "f78df0cac5e40c02d4e518ca6eaac8d82f01b721",
  "0760474f36e8b5359309cc6273931bdb2a0df3db",
  "e4d58fed8a728e7eca0fa5f6b8a880627df7ffe0",
  "297c79bfbdabe898736a3566f893697b59048119",
  "4f309ffea518f32cf21449273d7cee9d91366825",
  "75250def91799e2786d3748421599e3e9c8fe21d",
  "a80270815fe85df2fbdaa35adf9c1e2a8a3c0ed1",
  "6bfe16849ef307590d273e34f98dff7e4c6428da",
  "8099f4efbacea67c7d1afcc4f14a3e3e04d42f8a",
  "c2acaf127972d33e5901a19bbd47d5552c7f47e8",
  "e80e952eb9d8e96cf37cb990c801f97b7684319e",
  "1b429ad564b858f9a3e247cb2c083eb8cb37f0a7",
  "2e9d34119f3374cebd4d3fd81b6ee7b3bb1c863e",
  "2601a7462667a40844853040b7a05814d32feb3e",
  "719e01fcd3fe22a4248ac9ed336de7daecd3ada8",
  "b4f2222d3b41a3dbd199b364f73bb387d080589a",
  "b054c24026cdea5b9a2145128edfed863bd39f91",
  "7c10696489a30fd54c7b2c1d0e2adcd93c0a5eb2",
  "d37dc2c9a7a5236bb4734865425feeaa4e2fe981",
  "b29ee11b922ce1e6af41e3a2517ee5bb9cda1a2a",
  "3c984a24b9c429ca42db0b956af67442931a4c45",
  "55e1db7e9e779f6bee9cd56481fb339258e4d27e",
  "b0d1cb7c2b70a3a4419f4fe020864d3979317de2",
  "3f0749d0b7d52b20cf1cb80b2b73a41ba5ef542e",
  "196161a9cf8169b1a83bdceca5ffb82d2d59a32a",
  "99ed5ebe1bd812cb504e1427bbc14ebbe24bca87",
  "305fc388e69f6342e5e2ab29955b73647f0bbe42",
  "29cfdd24a2eeb454d134955a7b92868492545a10",
  "2186d0f99f7c9e215edfe6a4aabc4b3a7e38e743",
  "19cd75aa65fef9f02ce76b119ff903d48bcb1c16",
  "b92ce8343cbab46c1114afe44aa5c9af9f0ba3d9",
  "0f871f5c471360ead4d6df146afca5eab8f67897",
  "996fafb893ccb49192be8f6688437717713daf34",
  "05dff69a912715d51cf591093a9ef4e863a5e850",
  "a965cda2c354fa708c7e8a908b713e95c939b774",
  "f4ebdf672eb231645ae36f2e1e4de1e90c80621d",
  "b212f19d54dbcecc24b35c47009edc77eb48631d",
  "076231e171ce761497aa7947d9815df1bcadd49c",
  "5f7794e1dd4c786a2eb2618c1266f6a90663f76c",
  "7a9ceb98bfe3fa6bad17408d946a7c7fa8ffe5b5",
  "4f511210d472406eb1ff00d00890d5334768b8c2",
  "bce779212cccf1052fda3176f812815a064c2957",
  "cac42b13d72aca08ef7bcd5c2972284c4cab3209",
  "eb83425ded302b2ac09dc275c54898f425d8d9f2",
  "b87f6e3490cacaead49a6fa5ca9f7ac8cb3650e6",
  "e92df49784dc2efcd1b237b51cad303877ebce4b",
  "0f39d234b9ae6fbf3eea29130a35755ade7c55dc",
  "06edc0668235ba6e38facc3bbe5924a37935b4cd",
];

export const SPRY_ID_POOL: readonly string[] = [
  "aaaa1111",
  "bbbb2222",
  "cccc3333",
  "dddd4444",
  "eeee5555",
  "ffff6666",
  "aaaa7777",
  "bbbb8888",
  "cccc9999",
  "ddddaaaa",
  "eeeebbbb",
  "ffffcccc",
  "aaaadddd",
  "bbbbeeee",
  "ccccffff",
  "dddd5555",
  "eeee6666",
  "ffff7777",
];

/**
 * Compute a stable, order-independent index into a pool for the given hex
 * string.  Uses a simple djb2-style hash so that the same real SHA always
 * maps to the same pool entry regardless of the order in which SHAs are
 * encountered across test runs.  Linear probing resolves collisions.
 */
function hashIndex(hex: string, poolSize: number, occupied: Set<number>): number {
  let h = 5381;
  for (let i = 0; i < hex.length; i++) {
    // eslint-disable-next-line no-bitwise
    h = ((h << 5) + h + hex.charCodeAt(i)) >>> 0;
  }
  let idx = h % poolSize;
  while (occupied.has(idx)) {
    idx = (idx + 1) % poolSize;
  }
  return idx;
}

export function buildShaMap(shas: string[]): Map<string, string> {
  const map = new Map<string, string>();
  const occupied = new Set<number>();
  const unique = [...new Set(shas)];
  if (unique.length > SHA_POOL.length) {
    throw new Error(
      `SHA_POOL exhausted — ${unique.length} unique SHAs needed but pool only has ${SHA_POOL.length} entries.\n` +
        `Add more 40-char entries to SHA_POOL in tests/lib/sha-scanner.ts.`,
    );
  }
  for (const sha of unique) {
    const idx = hashIndex(sha, SHA_POOL.length, occupied);
    occupied.add(idx);
    map.set(sha, SHA_POOL[idx] as string);
  }
  return map;
}

export function buildSpryMap(spryIds: string[]): Map<string, string> {
  const map = new Map<string, string>();
  const occupied = new Set<number>();
  const unique = [...new Set(spryIds)];
  if (unique.length > SPRY_ID_POOL.length) {
    throw new Error(
      `SPRY_ID_POOL exhausted — ${unique.length} unique Spry-Commit-Ids needed but pool only has ${SPRY_ID_POOL.length} entries.\n` +
        `Add more 8-char entries to SPRY_ID_POOL in tests/lib/sha-scanner.ts.`,
    );
  }
  for (const id of unique) {
    const idx = hashIndex(id, SPRY_ID_POOL.length, occupied);
    occupied.add(idx);
    map.set(id, SPRY_ID_POOL[idx] as string);
  }
  return map;
}

function isHexChar(ch: string): boolean {
  return (ch >= "0" && ch <= "9") || (ch >= "a" && ch <= "f");
}

export function scanAndReplace(
  content: string,
  shaMap: Map<string, string>,
  spryMap: Map<string, string>,
): string {
  if (shaMap.size === 0 && spryMap.size === 0) return content;

  const shas = [...shaMap.keys()];

  let result = "";
  let i = 0;

  while (i < content.length) {
    if (!isHexChar(content.charAt(i))) {
      result += content.charAt(i++);
      continue;
    }

    // Consume hex run greedily up to 40 chars
    let j = i;
    while (j < content.length && j - i < 40 && isHexChar(content.charAt(j))) j++;
    const runLen = j - i;

    if (runLen < 6) {
      result += content.slice(i, j);
      i = j;
      continue;
    }

    // Try longest match first, down to min 6
    let matched = false;
    for (let len = runLen; len >= 6; len--) {
      const candidate = content.slice(i, i + len);

      // Spry-Commit-Ids are always exactly 8 chars — check at len === 8 only
      if (len === 8) {
        const spryFake = spryMap.get(candidate);
        if (spryFake) {
          result += spryFake;
          i += len;
          matched = true;
          break;
        }
      }

      // SHA: any registered SHA that starts with this candidate
      const sha = shas.find((s) => s.startsWith(candidate));
      if (sha) {
        const fakeSha = shaMap.get(sha);
        if (!fakeSha)
          throw new Error(`sha-scanner: invariant violation — sha key missing from map`);
        result += fakeSha.slice(0, len);
        i += len;
        matched = true;
        break;
      }
    }

    if (!matched) {
      result += content[i];
      i++;
    }
  }

  return result;
}
